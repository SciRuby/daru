module Daru
  # DateTimeIndex is an kind of index with each label being of Time, Date or DateTime class.
  #
  # In addition to usual index operations, it allows slicing by date components (year, month, day),
  # and, if ordered, by non-exact date ranges.
  #
  # @example
  class DateTimeIndex
  end
end
